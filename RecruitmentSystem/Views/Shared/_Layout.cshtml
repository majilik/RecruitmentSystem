<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - @Resources._Layout.SystemName</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink(Resources._Layout.SystemName, "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    @{if (HttpContext.Current.User.Identity.IsAuthenticated)
                        {
                            <li><p class="navbar-text">@Resources._Layout.WelcomePhrase @HttpContext.Current.User.Identity.Name</p></li>
                            <li>@Html.ActionLink(Resources._Layout.SignOut, "SignOut", "Authentication")</li>
                        }
                        else
                        {
                            <li>@Html.ActionLink(Resources._Layout.Register, "Register", "Authentication")</li>
                            <li>@Html.ActionLink(Resources._Layout.LogIn, "Login", "Authentication")</li>
                        }
                    }
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">@Resources._Layout.Language <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            @foreach (RecruitmentSystem.Resources.Locales loc in Enum.GetValues(typeof(RecruitmentSystem.Resources.Locales)))
                            {
                                string localeName = RecruitmentSystem.Resources.LocalesExtension.ParseCultureInfo(loc).NativeName;
                                <li>@Html.ActionLink(localeName, "ChangeLocale", "Home", new { locale = loc }, null)</li>
                            }
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - @Resources._Layout.SystemName</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jquery-ui")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)

    <script type="text/javascript">
        $(document).ready(function () {
            $('.datepicker').datetimepicker({ format: 'l' });
        });
    </script>
</body>
</html>
